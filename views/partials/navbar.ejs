<head>
    <link href="/styles/partials/navbar.css" rel="stylesheet">
</head>
<body>
    <div class="navbar">

        <a href="/">Home</a>
        <a href="/upload">Upload</a>
        <a href="/admin">Admin</a>
        <% if(user.name!==" "||user.name){ %>
        <a><%=user.name%></a>
        <%}%>
        <% if(user.name==""||!user.name){%>
            <a href="/signup">Sign Up!</a>
            <%}%>
    </div>
</body>